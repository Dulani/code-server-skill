{
  "name": "code-server",
  "version": "1.0.0",
  "description": "Spin up code-server (VS Code in browser) as an ephemeral local file viewer for agent-generated artifacts. Auto port selection, healthz polling, browser auto-open, and idle timeout.",
  "repository": "https://code.rand.org/skills/code-server"
}
